<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>{{ renderData.title }}</title>
	<link rel="stylesheet" href="/css/style.css" />
    <link rel="icon" href="/images/chandler-crest.svg" type="image/svg+xml">
	<link rel="apple-touch-icon" sizes="180x180" href="images/chandler-crest.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="images/chandler-crest.svg">
    <link rel="icon" type="image/png" sizes="16x16" href="images/chandler-crest.svg">

	<link rel="canonical" href="{{ metadata.url | append: page.url }}" />

	<meta property="og:site_name" content="{{ renderData.title }}" />
	<meta property="og:title" content="{{ renderData.title }}" />
	<meta property="og:type" content="website" />
	<meta property="og:url" content="{{ metadata.url | append: page.url }}" />

	<meta name="twitter:card" content="summary_large_image" />
	<meta property="og:image" content="images/mallow.png" />
	<meta name="twitter:image" content="images/mallow.png" />
	<meta property="og:image:alt" content="Page image for {{ metadata.title }}" />
	<meta name="twitter:image:alt" content="Page image for {{ metadata.title }}" />
</head>
<body>
	<nav class="bg-gray-800 mx-auto px-2 py-2 sm:px-6 lg:px-8 lg:py-4">
		<ul class="flex justify-start">
			<li class="flex mr-4">
				<a href="/" class="flex-shrink-0 flex items-center">
					<img class="block h-12 w-auto" width="30px" height="30px" src="/images/chandler-crest.svg" alt="Chandler family crest" />
				</a>
			</li>
			<li class="flex mr-4 p-4 {% if page.url == "/" %}bg-white text-black{%else%}text-white{%endif%}">
				<a href="/" class="flex items-center hover:underline cursor-pointer">
					Posts
				</a>
			</li>
			<li class="flex mr-4 p-4 {% if page.url contains "/about" %}bg-white text-black{%else%}text-white{%endif%}">
				<a href="/about" class="flex items-center hover:underline cursor-pointer">
					About
				</a>
			</li>
		</ul>
	</nav>
	<main>
	{{ content }}
	</main>
	<footer class="bg-gray-800 text-white border-red-500 fixed inset-x-0 bottom-0 p-4">
	    <div class="container mx-auto px-6 flex flex-col items-center">
	        <p class="text-sm font-bold mb-2">
	            © {% if page.date %}{{ page.date | date: "%Y" }}{% else %}{{ "now" | date: "%Y" }}{% endif %} by Alan Chandler
	        </p>
	    </div>
	</footer>
</body>
</html>